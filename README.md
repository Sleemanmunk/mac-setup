# mac-setup

Install scripts for setting up a fresh macOS machine.

## Usage

Run scripts from the project root:

```bash
bash install-claude-code.sh
```

Each script is its own user-facing entry point and pulls in its dependencies automatically.
